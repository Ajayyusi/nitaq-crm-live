/* Superseded by the annunciator Lamp — kept as an alias so the old
   shadcn-style import path keeps working. */
export { Lamp as Badge, lampVariants as badgeVariants } from "./lamp";
export type { LampProps as BadgeProps } from "./lamp";
