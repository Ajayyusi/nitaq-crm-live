// The App Router runs on Next's bundled React canary, which exports
// <ViewTransition> (used for route transitions — see components/motion).
// The stable @types/react only declares it in its canary entry point.
/// <reference types="react/canary" />
